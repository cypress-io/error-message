'use strict'


/* global describe, it */
const cypressTerminalErrorRender = require('.')

describe('@cypress/cypress-terminal-error-render', () => {
  it('write this test', () => {
    console.assert(cypressTerminalErrorRender, 'should export something')
  })
})
